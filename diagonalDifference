def diagonalDifference(arr):
    total1 = 0
    total2 = 0
    counter = 0
    for row in arr:
        total1 += row[counter]
        counter += 1
    for row in arr:
        total2 += row[counter-1]
        counter -= 1
    return abs(total1 - total2)
