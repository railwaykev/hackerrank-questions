def birthdayCakeCandles(candles):
    count = 1
    sortedList = sorted(candles, reverse=True)
    for i in range(1, len(sortedList)):
        if sortedList[0] == sortedList[i]:
            count += 1
    return count
